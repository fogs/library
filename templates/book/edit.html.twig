{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block content %}
    <h1>Edit Book</h1>

    {{ include('book/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('book_index') }}" class="btn btn-primary">back to list</a>

    {{ include('book/_delete_form.html.twig') }}
{% endblock %}
