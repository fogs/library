{% extends 'base-avanzu.html.twig' %}

 
{% block page_title %} Author {% endblock %}
 
{% block page_content %}
<div class="row">  
<div class="col-md-12">


    <div class="box box-primary">
            <div class="box-header with-border">
              <h3 class="box-title">Author</h3>
               {{ include('author/_delete_form.html.twig') }}
              <a class="btn btn-success pull-right" href="{{ path('author_edit', {'id': author.id}) }}"><i class="fa fa-edit"></i></a>&nbsp;
              
            </div>
  

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ author.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ author.name }}</td>
            </tr>
        </tbody>
    </table>

    

    </div><!-- box -->
    </div><!-- col-md-12 -->

    </div><!-- row -->
    <a href="{{ path('author_index') }}"><i class="fa fa-arrow-left">Back to list</i></a>
{% endblock %}
